const config = {
    development: {
        username: "postgres",
        password: "postgres",
        database: "dvd",
        host: "127.0.0.1",
        dialect: "postgres"
    }
}

module.exports = config;